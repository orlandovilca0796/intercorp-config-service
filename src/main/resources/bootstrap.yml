server:
  port: 8080

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/orlandovilca0796/config-service-intercorp.git
          defaultLabel: master
  security:
    user:
      name: root
      password: s3cr3t